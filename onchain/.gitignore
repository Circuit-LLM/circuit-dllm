.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
# keypairs are environment secrets — never commit (regenerate per the README)
keys/*.json
# local ops helpers (the deploy command is documented in README.md)
keys/*.sh
